import { styled } from "@mui/material/styles";
import Typography from "@mui/material/Typography";

export const NotebookHeading = styled(Typography)({
  position: "relative",
  zIndex: 1,
  width: "fit-content",
  fontWeight: 800,
}) as typeof Typography;
